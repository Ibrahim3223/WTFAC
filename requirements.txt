# ============================================================
# YouTube Shorts Automation - Requirements (Production Ready)
# ============================================================

# --- Core AI/ML (Gemini SDKs) ---
google-genai>=0.2.0           # import: google.genai
google-generativeai>=0.8.3    # import: google.generativeai

# --- Config / Validation / Env ---
pyyaml>=6.0
pydantic>=2.0
python-dotenv>=1.0.0

# --- Text-to-Speech ---
edge-tts>=6.1.0
nest-asyncio>=1.5.0
gtts>=2.5.0

# --- Google APIs (YouTube upload) ---
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.1.0
google-auth>=2.23.0
google-auth-httplib2>=0.1.1

# --- HTTP / Feeds ---
requests>=2.31.0
feedparser>=6.0.0

# --- Image/Video processing ---
Pillow>=10.0.0

# --- Audio/Video utilities ---
pydub>=0.25.0   # Requires FFmpeg runtime (system package)

# --- Numeric / ML toolstack ---
numpy>=1.26,<2
scikit-learn>=1.3.0

# --- ML / Embeddings ---
sentence-transformers>=2.7.0

# --- CLI / Logging (optional but useful) ---
tqdm>=4.66.0
colorlog>=6.8.0

# ============================================================
# MILISANIYE HASSASİYETİNDE CAPTION SYNC - WhisperX
# ============================================================
# WhisperX: Whisper + forced alignment = phoneme-level precision
# ~10ms hassasiyet - izleyicinin gözünü yormayan mükemmel sync!
#
# WhisperX forced alignment sistemi:
# 1. Whisper ile transcription
# 2. wav2vec2 ile phoneme-level alignment
# 3. Her kelime için start/end timestamps (milisaniye precision)
#
# Installation:
# pip install git+https://github.com/m-bain/whisperx.git
#
# WhisperX dependencies (otomatik kurulur):
whisperx>=3.1.1
torch>=2.0.0
torchaudio>=2.0.0
faster-whisper>=0.10.0
transformers>=4.30.0
# pyannote.audio>=3.0.0  # Optional: speaker diarization için

# ============================================================
# System Requirements:
# ============================================================
# - FFmpeg (video/audio processing)
# - CUDA (optional, GPU acceleration için - CPU'da da çalışır)
# - Model download: First run'da WhisperX modelleri indirilir (~1-2GB)
#
# GPU varsa: ~2-3x daha hızlı
# CPU'da: Biraz yavaş ama çalışır
#
# ============================================================
# Fallback Strategy (3-layer system):
# ============================================================
# 1. WhisperX forced alignment (BEST - phoneme-level precision)
#    - Real audio analysis
#    - ~10ms accuracy
#    - GPU/CPU compatible
#
# 2. Edge-TTS word timings (GOOD - TTS engine boundaries)
#    - TTS engine word boundaries
#    - ~50-100ms accuracy
#    - No extra processing
#
# 3. Character-based estimation (FALLBACK - last resort)
#    - Word length based distribution
#    - ~200ms accuracy
#    - Always works
# ============================================================
