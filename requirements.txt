# ============================================================
# YouTube Shorts Automation - Requirements (merged)
# ============================================================

# --- Core AI/ML (Gemini SDKs: yalnızca birini kullanıyorsanız diğerini kaldırın) ---
google-genai>=0.2.0           # import: google.genai
google-generativeai>=0.8.3    # import: google.generativeai

# --- Config / Validation / Env ---
pyyaml>=6.0
pydantic>=2.0
python-dotenv>=1.0.0

# --- Text-to-Speech ---
edge-tts>=6.1.0
nest-asyncio>=1.5.0
gtts>=2.5.0

# --- Google APIs (YouTube upload) ---
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.1.0
google-auth>=2.23.0
google-auth-httplib2>=0.1.1

# --- HTTP / Feeds ---
requests>=2.31.0
feedparser>=6.0.0

# --- Image/Video processing ---
Pillow>=10.0.0

# --- Audio/Video utilities ---
pydub>=0.25.0   # FFmpeg runtime gerekir (sistem paketi)

# --- Numeric / ML toolstack ---
numpy>=1.26,<2
scikit-learn>=1.3.0

# --- ML / Embeddings ---
sentence-transformers>=2.7.0   # (daha yeni gereksinim olduğu için 2.7.0'ı koruduk)

# --- CLI / Logging (opsiyonel ama faydalı) ---
tqdm>=4.66.0
colorlog>=6.8.0

# ============================================================
# Perfect Caption Sync (Whisper) — etkin kullanıyorsanız yükleyin
# ============================================================
faster-whisper>=1.0.0
av>=11.0.0                  # PyAV: faster-whisper için gerekli
openai-whisper>=20231117    # Fallback olarak (opsiyonel, isterseniz kaldırın)

# Notlar:
# - 'pydub' ve Whisper için FFmpeg ikilisi gereklidir (sistem düzeyinde).
# - Eğer yalnızca tek Gemini SDK'sı kullanıyorsanız, diğeri satırı silinebilir.
