# Core TTS and async
edge-tts
nest-asyncio

# HTTP requests
requests

# Google APIs (YouTube upload)
google-api-python-client
google-auth-oauthlib
google-auth
google-auth-httplib2

# Image processing
Pillow>=10.0.0

# Numeric computing
numpy>=1.26,<2

# ML/Embeddings (for state management - optional but recommended)
sentence-transformers==2.7.0
scikit-learn>=1.3.0

# RSS/Feed parsing (if used elsewhere)
feedparser

# YAML parsing (for channels.yml)
pyyaml>=6.0
